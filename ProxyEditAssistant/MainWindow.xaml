<Window x:Class="ProxyEditAssistant.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Height="450"
        Width="800">
    <StackPanel>
        <Button Content="Create Proxies" Click="CreateProxies" />
        <StackPanel>
            <Label Content="Progress" HorizontalAlignment="Center" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Label Grid.Row="0" Grid.Column="0" Content="Current File" />
                <Label Grid.Row="0" Grid.Column="1" Content="{Binding FileCount}" />
                <Label Content="{Binding TotalFiles}" />
                <Label Content="{Binding BitRate}" />
                <Label Content="{Binding FramesPerSecond}" />
            </Grid>
        </StackPanel>
    </StackPanel>
</Window>